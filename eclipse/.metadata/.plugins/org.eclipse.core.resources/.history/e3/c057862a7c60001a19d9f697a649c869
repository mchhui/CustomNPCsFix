package mchhui.customnpcsfix;

import java.io.File;
import java.io.UnsupportedEncodingException;

import org.bukkit.Bukkit;
import org.bukkit.plugin.InvalidDescriptionException;
import org.bukkit.plugin.InvalidPluginException;
import org.bukkit.plugin.UnknownDependencyException;

import noppes.npcs.Server;

public class BukkitLoader {
    public static void load() throws UnknownDependencyException, InvalidPluginException, InvalidDescriptionException {
        if (Bukkit.getServer() == null) {
            return;
        }
        Bukkit.getPluginManager().loadPlugin(new File(System.getProperty("java.class.path")));
    }
}
